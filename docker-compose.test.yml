services:
  psql_monexa:
    container_name: psql_monexa_test
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_NAME:-monexa}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "127.0.0.1:${DB_PORT:-5433}:5432"
    volumes:
      - psql_volume_monexa_test:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} -d ${DB_NAME:-monexa}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - monexa-network
    restart: unless-stopped

  backend:
    container_name: monexa_backend_test
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: ${APP_ENV:-test}
      SERVER_PORT: ${SERVER_PORT:-9000}
      DB_HOST: psql_monexa_test
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-monexa}
      DB_USERNAME: ${DB_USERNAME:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_SSLMODE: ${DB_SSLMODE:-disable}
      JWT_SECRET: ${JWT_SECRET:-monexasupersecretkeythatismorethan32byteslong!}
      ACCESS_TOKEN_DURATION: ${ACCESS_TOKEN_DURATION:-168h}
      REFRESH_TOKEN_DURATION: ${REFRESH_TOKEN_DURATION:-720h}
      EXCHANGE_RATE_API_KEY: ${EXCHANGE_RATE_API_KEY:-monexaexchangerateapikey}
      RESEND_API_KEY: ${RESEND_API_KEY:-monexaresendapikey}
      RESEND_FROM_NAME: ${RESEND_FROM_NAME:-Monexa}
      RESEND_FROM_ADDRESS: ${RESEND_FROM_ADDRESS:-no-reply@monexa.world}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      CORS_ORIGINS: ${CORS_ORIGINS}
      LEGAL_COMPLIANCE_ENABLED: ${LEGAL_COMPLIANCE_ENABLED:-false}
    ports:
      - "${SERVER_PORT:-9000}:9000"
    depends_on:
      psql_monexa:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - monexa-network
    restart: unless-stopped

  frontend:
    container_name: monexa_frontend_test
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:9000/api/v1}
        VITE_LEGAL_COMPLIANCE_ENABLED: ${VITE_LEGAL_COMPLIANCE_ENABLED:-false}
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    networks:
      - monexa-network
    restart: unless-stopped

  portainer:
    container_name: monexa_portainer_test
    image: portainer/portainer-ce:alpine
    ports:
      - "127.0.0.1:${PORTAINER_PORT:-9443}:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - portainer_data_monexa_test:/data
    networks:
      - monexa-network
    restart: unless-stopped

networks:
  monexa-network:
    driver: bridge

volumes:
  psql_volume_monexa_test:
  portainer_data_monexa_test:
